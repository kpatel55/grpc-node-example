This is a simple example of a unary gRPC server and client call.

## Getting Started

First, run the development server:

```bash
npm run server
```

Then run the client call: 

```bash
npm run client
```
